# models/marts/finance/schema.yml
version: 2

models:
  - name: fct_orders
    description: One row per order with payment amount
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
      - name: customer_id
        tests:
          - not_null
      - name: amount
        tests:
          - not_null